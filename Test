local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- Detect เวลา UI โผล่
PlayerGui.ChildAdded:Connect(function(child)
    -- ถ้าชื่อ UI มีคำว่า "Dialogue" หรือ "Chat" ให้ลองเช็ก
    if string.find(string.lower(child.Name), "dialogue") or string.find(string.lower(child.Name), "chat") then
        warn("✅ เริ่มคุยกับ NPC แล้ว → พยายามกด Option 1")
        
        task.wait(0.5) -- รอ UI โหลดครบ

        -- หาปุ่มตัวเลือก
        for _, obj in pairs(child:GetDescendants()) do
            if obj:IsA("TextButton") and string.find(obj.Text, "appraise") then
                task.wait(0.2)
                obj:Activate() -- หรือ obj.MouseButton1Click:Fire() 
                warn("🎯 กดเลือก: Can you appraise this fish?")
            end
        end
    end
end)

-- Detect เวลาเลิกคุย
PlayerGui.ChildRemoved:Connect(function(child)
    if string.find(string.lower(child.Name), "dialogue") or string.find(string.lower(child.Name), "chat") then
        warn("❌ จบบทสนทนาแล้ว")
    end
end)
