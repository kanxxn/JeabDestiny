-- 🐥 Auto Scylla Hunt (Detect จาก Model + Debug)
-- Made By Jeab 🐥

local sundialName = "Sundial Totem"
local rodName = "Blessed Rod Of The Zenith"

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local isRunning = true -- เปิดอัตโนมัติ (กดปิดเองก็ได้)
local scyllaActive = false

print("===== DEBUG SCYLLA SPAWN TRACKER START =====")

-- ฟังก์ชันใช้ไอเท็ม
local function useItem(itemName)
    local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoid = char:WaitForChild("Humanoid")
    local tool = char:FindFirstChildOfClass("Tool")

    if tool and tool.Name == itemName then
        tool:Activate()
        return true
    end

    local backpack = LocalPlayer.Backpack
    local toolInBackpack = backpack:FindFirstChild(itemName)
    if toolInBackpack then
        humanoid:EquipTool(toolInBackpack)
        task.wait()
        toolInBackpack:Activate()
        return true
    end
    return false
end

-- ฟังก์ชันสลับถือ
local function equipItem(itemName)
    local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoid = char:WaitForChild("Humanoid")
    local tool = char:FindFirstChildOfClass("Tool")

    if tool and tool.Name == itemName then
        return true
    end

    local backpack = LocalPlayer.Backpack
    local toolInBackpack = backpack:FindFirstChild(itemName)
    if toolInBackpack then
        humanoid:EquipTool(toolInBackpack)
        return true
    end
    return false
end

-- ✅ Debug + Detect Scylla
workspace.ChildAdded:Connect(function(child)
    print("[DEBUG] ChildAdded:", child.Name, child.ClassName)

    -- ถ้ามีคำว่า "Scylla" ในชื่อ = ถือ Rod
    if string.find(string.lower(child.Name), "scylla") then
        scyllaActive = true
        print("[DEBUG] พบ Scylla → ถือ Rod")
        equipItem(rodName)
    end
end)

workspace.ChildRemoved:Connect(function(child)
    print("[DEBUG] ChildRemoved:", child.Name, child.ClassName)

    if string.find(string.lower(child.Name), "scylla") then
        scyllaActive = false
        print("[DEBUG] Scylla หายไปแล้ว → ใช้ Sundial")
        useItem(sundialName)
    end
end)

-- ลูปหลัก
task.spawn(function()
    while isRunning do
        if not scyllaActive then
            useItem(sundialName)
        else
            equipItem(rodName)
            repeat task.wait() until not isRunning or not scyllaActive
        end
        task.wait()
    end
end)
