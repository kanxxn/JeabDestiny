-- Auto Scylla Hunt GUI + Debug Logger
-- Made By Jeab 🐥

--== Services ==--
local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")

local player = Players.LocalPlayer
local backpack = player:WaitForChild("Backpack")

--== GUI ==--
local ScreenGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))

-- Main GUI
local mainFrame = Instance.new("Frame", ScreenGui)
mainFrame.Size = UDim2.new(0, 250, 0, 160)
mainFrame.Position = UDim2.new(0, 50, 0, 200)
mainFrame.BackgroundColor3 = Color3.fromRGB(255, 204, 102)
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Visible = true
mainFrame.BackgroundTransparency = 0.05
mainFrame.BorderSizePixel = 0
mainFrame.ClipsDescendants = true
mainFrame.ZIndex = 2
mainFrame.Name = "MainFrame"
mainFrame.Parent = ScreenGui
mainFrame.UICorner = Instance.new("UICorner", mainFrame)

-- Title
local title = Instance.new("TextLabel", mainFrame)
title.Size = UDim2.new(1, -50, 0, 30)
title.Position = UDim2.new(0, 10, 0, 5)
title.BackgroundTransparency = 1
title.Font = Enum.Font.SourceSansBold
title.Text = "🐥 Auto Scylla Hunt 🐥"
title.TextSize = 20
title.TextColor3 = Color3.new(0, 0, 0)
title.TextXAlignment = Enum.TextXAlignment.Left

-- Close Button (X)
local closeBtn = Instance.new("TextButton", mainFrame)
closeBtn.Size = UDim2.new(0, 30, 0, 30)
closeBtn.Position = UDim2.new(1, -35, 0, 5)
closeBtn.Text = "X"
closeBtn.Font = Enum.Font.SourceSansBold
closeBtn.TextSize = 18
closeBtn.TextColor3 = Color3.new(1, 1, 1)
closeBtn.BackgroundColor3 = Color3.fromRGB(255, 100, 100)
closeBtn.UICorner = Instance.new("UICorner", closeBtn)

-- Minimize Button (-)
local minimizeBtn = Instance.new("TextButton", mainFrame)
minimizeBtn.Size = UDim2.new(0, 30, 0, 30)
minimizeBtn.Position = UDim2.new(1, -70, 0, 5)
minimizeBtn.Text = "-"
minimizeBtn.Font = Enum.Font.SourceSansBold
minimizeBtn.TextSize = 18
minimizeBtn.TextColor3 = Color3.new(1, 1, 1)
minimizeBtn.BackgroundColor3 = Color3.fromRGB(100, 255, 100)
minimizeBtn.UICorner = Instance.new("UICorner", minimizeBtn)

-- Start Button
local startBtn = Instance.new("TextButton", mainFrame)
startBtn.Size = UDim2.new(0.8, 0, 0, 50)
startBtn.Position = UDim2.new(0.1, 0, 0, 50)
startBtn.Text = "เริ่มทำงาน"
startBtn.Font = Enum.Font.SourceSansBold
startBtn.TextSize = 20
startBtn.TextColor3 = Color3.new(1, 1, 1)
startBtn.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
startBtn.UICorner = Instance.new("UICorner", startBtn)

-- Debug Button
local debugBtn = Instance.new("TextButton", mainFrame)
debugBtn.Size = UDim2.new(0, 80, 0, 30)
debugBtn.Position = UDim2.new(0.05, 0, 1, -40)
debugBtn.Text = "Debug"
debugBtn.Font = Enum.Font.SourceSansBold
debugBtn.TextSize = 16
debugBtn.TextColor3 = Color3.new(1, 1, 1)
debugBtn.BackgroundColor3 = Color3.fromRGB(120, 120, 255)
debugBtn.UICorner = Instance.new("UICorner", debugBtn)

-- Credit
local credit = Instance.new("TextLabel", mainFrame)
credit.Size = UDim2.new(1, -10, 0, 20)
credit.Position = UDim2.new(0, 5, 1, -20)
credit.BackgroundTransparency = 1
credit.Font = Enum.Font.SourceSansItalic
credit.Text = "Made By Jeab 🐥"
credit.TextSize = 14
credit.TextColor3 = Color3.new(0, 0, 0)
credit.TextXAlignment = Enum.TextXAlignment.Right

--== Debug Frame ==--
local debugFrame = Instance.new("Frame", ScreenGui)
debugFrame.Size = UDim2.new(0, 300, 0, 250)
debugFrame.Position = UDim2.new(1, -320, 0, 200)
debugFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
debugFrame.Visible = false
debugFrame.Active = true
debugFrame.Draggable = true
debugFrame.BorderSizePixel = 0
debugFrame.UICorner = Instance.new("UICorner", debugFrame)

local debugTitle = Instance.new("TextLabel", debugFrame)
debugTitle.Size = UDim2.new(1, -60, 0, 30)
debugTitle.Position = UDim2.new(0, 10, 0, 5)
debugTitle.BackgroundTransparency = 1
debugTitle.Font = Enum.Font.SourceSansBold
debugTitle.Text = "🔍 Debug Log"
debugTitle.TextSize = 18
debugTitle.TextColor3 = Color3.new(1, 1, 1)
debugTitle.TextXAlignment = Enum.TextXAlignment.Left

local copyBtn = Instance.new("TextButton", debugFrame)
copyBtn.Size = UDim2.new(0, 60, 0, 30)
copyBtn.Position = UDim2.new(1, -70, 0, 5)
copyBtn.Text = "Copy"
copyBtn.Font = Enum.Font.SourceSansBold
copyBtn.TextSize = 16
copyBtn.TextColor3 = Color3.new(1, 1, 1)
copyBtn.BackgroundColor3 = Color3.fromRGB(50, 200, 100)
copyBtn.UICorner = Instance.new("UICorner", copyBtn)

local logBox = Instance.new("TextBox", debugFrame)
logBox.Size = UDim2.new(1, -20, 1, -50)
logBox.Position = UDim2.new(0, 10, 0, 40)
logBox.MultiLine = true
logBox.TextWrapped = false
logBox.TextXAlignment = Enum.TextXAlignment.Left
logBox.TextYAlignment = Enum.TextYAlignment.Top
logBox.ClearTextOnFocus = false
logBox.Font = Enum.Font.Code
logBox.TextSize = 14
logBox.TextColor3 = Color3.new(1, 1, 1)
logBox.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
logBox.Text = ""

--== Functions ==--
local running = false

local function log(msg)
    logBox.Text = msg .. "\n" .. logBox.Text
end

local function equipRod()
    local rod = backpack:FindFirstChildWhichIsA("Tool")
    if rod then
        player.Character.Humanoid:EquipTool(rod)
        log("พบ Scylla → ถือ Rod (Slot1)")
    else
        log("❌ ไม่มี Rod ใน Backpack")
    end
end

-- Workspace listener
workspace.ChildAdded:Connect(function(child)
    log("[+] " .. child.Name)
    if string.find(string.lower(child.Name), "scylla") and running then
        equipRod()
    end
end)

workspace.ChildRemoved:Connect(function(child)
    log("[-] " .. child.Name)
end)

--== Buttons ==--
closeBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

minimizeBtn.MouseButton1Click:Connect(function()
    mainFrame.Visible = not mainFrame.Visible
end)

startBtn.MouseButton1Click:Connect(function()
    running = not running
    if running then
        startBtn.Text = "หยุดทำงาน"
        startBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        log("▶ เริ่ม Auto Scylla Hunt")
    else
        startBtn.Text = "เริ่มทำงาน"
        startBtn.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
        log("⏸ หยุด Auto Scylla Hunt")
    end
end)

debugBtn.MouseButton1Click:Connect(function()
    debugFrame.Visible = not debugFrame.Visible
end)

copyBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard(logBox.Text)
        log("✅ Debug log copied!")
    end
end)
